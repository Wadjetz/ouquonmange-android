<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_calendar"
            android:icon="@drawable/ic_event_black"
            android:title="@string/calendar" />
        <item
            android:id="@+id/nav_communities"
            android:icon="@drawable/ic_group_black"
            android:title="@string/communities" />
        <item
            android:id="@+id/nav_search_communities"
            android:icon="@drawable/ic_group_add_black"
            android:title="@string/search_communities" />
    </group>

    <item android:title="@string/parameters" android:id="@+id/nav_parameters">
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_menu_manage"
                android:title="@string/action_settings" />
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_power_settings"
                android:title="@string/logout" />
        </menu>
    </item>

</menu>
